{
  "name": "mobile-developer-site",
  "version": "1.0.0",
  "description": "Mobile developer website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Building static site...' && mkdir -p dist && cp -r src/* dist/ && echo 'Build complete!'",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
