{
  "name": "mobile-developer-site",
  "version": "1.0.0",
  "description": "Mobile developer website with Express server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Static site - copying files' && mkdir -p dist && cp -r *.html *.png *.svg *.xml *.txt dist/ 2>/dev/null || true",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
